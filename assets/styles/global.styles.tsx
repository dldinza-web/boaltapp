import { StyleSheet } from "react-native"

const globalStyles = StyleSheet.create({
  boderDebug: {
    borderWidth: 1,
    borderColor: 'blue',
    borderStyle: "solid"
  }
})

export default globalStyles
